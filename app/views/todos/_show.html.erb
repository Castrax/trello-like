<div id="shared" data-id="<%= todo.id %>">
  <div class="card mb-2" data-id="<%= todo.id %>">
    <div class="card-body">
      <h5 class="card-title"><%= todo.title %></h5>
      <p class="card-text"><%= todo.description %></p>
    </div>
  </div>
</div>
